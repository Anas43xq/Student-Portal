# Backend environment example
# NOTE: These are provided as an example for local testing only. Do NOT commit real secrets.
# Store real credentials in an ignored file (e.g., Configurations.env) or as environment variables with proper access controls.
NODE_ENV=production
CORS_ORIGIN=https://your-frontend-domain.example
JWT_SECRET=your_super_secure_jwt_secret_key_here
BCRYPT_SALT_ROUNDS=10
DB_HOST=sql5.freesqldatabase.com
DB_USER=sql5812469
DB_PASSWORD=QB5vzAA7Qr
DB_NAME=sql5812469
DB_PORT=3306
